<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.mobop.mse.vtrack" >

    <!-- Used for communication with BaaS Backend -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"
            android:label="@string/activity_login_title"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity android:name=".SignupActivity"
            android:label="@string/activity_signup_title"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity android:name=".NewVoucherActivity"
            android:label="@string/activity_newVoucher_title"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            </activity>
        <activity android:name=".DetailVoucherActivity"
            android:label="@string/activity_voucherDetails_title"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity android:name=".SettingsActivity"
            android:label="@string/activity_settings_title"
            android:windowSoftInputMode="stateHidden">
        </activity>
    </application>

</manifest>
